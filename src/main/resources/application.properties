cat > src/main/resources/application.properties <<'EOF'
# =====================================
# APLICAÇÃO
# =====================================
spring.application.name=concurseiro-api
app.admin.api-key=${APP_ADMIN_API_KEY}

# =====================================
# BANCO (PostgreSQL)
# =====================================
spring.datasource.url=jdbc:postgresql://localhost:5432/concurseiro
spring.datasource.username=mauricio
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# =====================================
# SEM LIQUIBASE (ABORTADO)
# =====================================
spring.liquibase.enabled=false

# =====================================
# JPA / HIBERNATE
# =====================================
# Hibernate cria/atualiza as tabelas automaticamente no Postgres.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
EOF